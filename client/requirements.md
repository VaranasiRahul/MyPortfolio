## Packages
framer-motion | Complex animations for page transitions and scroll reveals
react-scroll | Smooth scrolling for single-page navigation
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The application is a single-page portfolio.
Data is fetched from the backend (seeded data).
Animations should be subtle and professional.
Using Lucide React for icons (already in base stack).
